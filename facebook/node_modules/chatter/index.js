var all_sockets = null;


exports.set_sockets = function (sockets) {
	all_sockets = sockets;
};


exports.connect_chatter = function (config) {

	//config.socket.emit('entrance'); //, {message: 'Welcome to the chat room!'});
	//all_sockets.emit('entrance');//, {message: config.username + ' is online.'});
	//all_sockets.emit('users', {name: config.username + ' '});
	//all_sockets.emit('current', {currentUser: socket});
/*
	var room = "abc123";
//	config.sockets.in(room).emit('message', {message:'what is going on, party people?'});


	config.socket.on('disconnect', function () {
		all_sockets.emit('exit');//, {message: config.username + ' has disconnected.'});
	});

	config.socket.on('chat', function (data) {
		//room = data.room;
//    	all_sockets.in('room1').emit('chat', {message: data.message + " "});
	   	all_sockets.emit('chat', {message: data.message + " "});

	});


	config.socket.on('room', function(room){
			socket.join(room);
			socket.broadcast.to(room).emit('joined', {pre_msg: currentUser + " is ready to play!"});
	});


	config.socket.on('ready', function (data){
		//room = data.room
//		config.socket.join(room);
//		config.socket.emit('ready', {message: data.message});
		socket.broadcast.to(room).emit('joined', {pre_msg: currentUser + " is ready to play!"});
	});

	config.socket.on('example', function (data) {
	    socket.emit('example', {test: data.test});
	});

	config.socket.on('addGame', function (data){
		socket.join('room1', function (data){
			socket.broadcast.to('room1').emit('joined', {pre_msg: currentUser + " is ready to play!"});
		});
	});
*/

};

exports.failure = function (socket) {
	socket.emit('error', {message: 'Please log in to the chatroom'});
};